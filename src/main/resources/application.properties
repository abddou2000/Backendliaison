# ==========================================
#      CONFIGURATION DE L'APPLICATION
# ==========================================
spring.application.name=Login

# ==========================================
#      CONFIGURATION BASE DE DONNÉES
# ==========================================
spring.datasource.url=jdbc:postgresql://localhost:5432/OUTIL_PAIE
spring.datasource.username=postgres
spring.datasource.password=12345
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
#      CONFIGURATION JPA & HIBERNATE
# ==========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==========================================
#      CONFIGURATION EMAIL (GMAIL)
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=abdoubensalek7@gmail.com
spring.mail.password=luzaytjzozedqqfp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==========================================
#      CONFIGURATION SWAGGER / SPRINGDOC
# ==========================================
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# ==========================================
#      CONFIGURATION JSON (JACKSON)
# ==========================================
spring.jackson.default-property-inclusion=NON_NULL

# ==========================================
#      CONFIGURATION DES LOGS
# ==========================================
logging.level.org.springframework.security=DEBUG
logging.level.org.springdoc=WARN
logging.level.io.swagger.models.parameters=ERROR

# ==========================================
#      CONFIGURATION SAUVEGARDE BDD
# ==========================================
# Répertoire où seront stockées les sauvegardes
backup.directory=./backups

# Chemin de pg_dump (détecté automatiquement si non spécifié)
backup.pg_dump.path=pg_dump

# Planification des sauvegardes automatiques (tous les jours à 2h du matin)
backup.cron=0 0 2 * * ?
? Sauvegarde réussie: backup_OUTIL_PAIE_20251031_233000.sql